<h2><%=@artist.name%></h2>
Songs:
<%@artist.songs.each do |song|%>
  <h4><a href="/songs/<%= song.slug %>"><%=song.name%></a></h4>
<%end%>

Genres:

<%@artist.genres.each do |genre|%>
  <h4><a href="/genres/<%= genre.slug %>"><%=genre.name%></a></h4>
<%end%>
